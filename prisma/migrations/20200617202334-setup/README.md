# Migration `20200617202334-setup`

This migration has been generated by nonissue at 6/17/2020, 8:23:34 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Post" (
"authorId" integer   ,"content" text   ,"id" SERIAL,"published" boolean  NOT NULL DEFAULT false,"title" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."User" (
"email" text  NOT NULL ,"id" SERIAL,"name" text   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Late" (
"authorId" integer   ,"id" SERIAL,"title" text   ,"url" text   ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Tag" (
"id" SERIAL,"title" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."_LateToTag" (
"A" integer  NOT NULL ,"B" integer  NOT NULL )

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

CREATE UNIQUE INDEX "_LateToTag_AB_unique" ON "public"."_LateToTag"("A","B")

CREATE  INDEX "_LateToTag_B_index" ON "public"."_LateToTag"("B")

ALTER TABLE "public"."Post" ADD FOREIGN KEY ("authorId")REFERENCES "public"."User"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Late" ADD FOREIGN KEY ("authorId")REFERENCES "public"."User"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."_LateToTag" ADD FOREIGN KEY ("A")REFERENCES "public"."Late"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."_LateToTag" ADD FOREIGN KEY ("B")REFERENCES "public"."Tag"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200617202334-setup
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,40 @@
+datasource db {
+  provider = "postgresql"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Post {
+  id        Int     @id @default(autoincrement())
+  title     String
+  content   String?
+  published Boolean @default(false)
+  author    User?   @relation(fields: [authorId], references: [id])
+  authorId  Int?
+}
+
+model User {
+  id    Int     @id @default(autoincrement())
+  email String  @unique
+  name  String?
+  posts Post[]
+  lates Late[]
+}
+
+model Late {
+  id       Int     @id @default(autoincrement())
+  title    String?
+  author   User?   @relation(fields: [authorId], references: [id])
+  authorId Int?
+  url      String?
+  tags     Tag[]   @relation(references: [id])
+}
+
+model Tag {
+  id    Int    @id @default(autoincrement())
+  title String
+  lates Late[] @relation(references: [id])
+}
```


